bundle:
  name: hl7_streaming_pipeline

workspace:
  profile: e2-demo-field-eng

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    variables:
      catalog: users
      schema: ankur_nayyar
      source_volume: landing
      funke_wheel_name: funke-0.1.0a1-py3-none-any.whl

  staging:
    variables:
      catalog: users
      schema: ankur_nayyar
      source_volume: landing
      funke_wheel_name: funke-0.1.0a1-py3-none-any.whl

  prod:
    mode: production
    variables:
      catalog: users
      schema: ankur_nayyar
      source_volume: landing
      funke_wheel_name: funke-0.1.0a1-py3-none-any.whl
