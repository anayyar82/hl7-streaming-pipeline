bundle:
  name: hl7_streaming_pipeline

workspace:
  profile: e2-demo-field-eng

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      root_path: /Workspace/Users/ankur.nayyar@databricks.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: ankur_nayyar_hl7
      schema: hl7_streaming
      source_volume: landing
